---
name: zhizhi
description: "元认知'先查后做'。打破AI智能体'埋头硬推、不检索、硬编造'的封闭执行：先按任务轻重分级，再扫描信息缺口、自适应路由检索（项目内Glob/Grep→官方文档→委托可用调研skill）、校验方案的版本/许可证，查不到就显式标注假设、绝不编造。触发：代码/方案/调研任务未检索就动手；连续多轮'可能/大概'空转不引外部信息；缺版本号/API文档/行业数据仍硬输出；在重造可能已有开源库的轮子；用'应该是'填充关键事实。不适用：纯数学推导、纯创意写作、用户明确说不用查、琐碎单行修改。"
allowed-tools: WebSearch WebFetch Bash Read Write Edit Glob Grep AskUserQuestion
shell: powershell
when_to_use: "任务涉及代码/方案/调研但尚未检索现有资源；连续2轮以上在同一问题空转推理；上下文缺版本/API/数据等关键信息仍继续输出；检测到'应该是/大概/通常'填充事实；在从头实现可能已有成熟方案的功能。"
not_for: "纯数学/逻辑推导（外部检索无增益）；纯创意写作（凭风格表达）；用户明确表示不需要检索；单行琐碎修改。"
metadata:
  version: "3.1-cc"
  evolution: "ascetic-breaker → 知止"
  platform: "方法论跨 agent；封装以 Claude Code 工具模型为参考"
  last_updated: "2026-08-12"
---

# 知止（Zhizhi）— 元认知"先查后做"

> **知止而后有定。** 三层"知止"：知道**何时停止埋头硬推**（转向外查）、知道**何时停止检索**（预算止损，不烧上下文）、知道**何时承认查不到**（假设显式化，不编造）。

## 定位：元认知交通警察，不是检索器

本 Skill **不重复实现检索逻辑**。它管的是检索**之外**的事。本 Skill 的**方法论跨 agent**（Claude Code / Codex / Cursor / OpenCode 等适用），但**当前封装以 Claude Code 工具模型为参考**（`allowed-tools`、工具命名、`shell: powershell` 均为 Claude Code 命名）——其他 agent 需要把工具名映射为各自等价能力，**不是复制即用**。

1. **分级（知轻重）**：判断任务该走多重的流程，轻任务不被流程压垮，重任务不漏关键缺口
2. **缺口检测（知缺什么）**：识别上下文缺什么、影响多大、该找谁补
3. **自适应路由（知去哪借）**：项目内 Glob/Grep → 官方文档 → 委托可用的调研 skill——**探测式**，不硬绑任何特定 skill
4. **融合校验（知借来的可不可信）**：版本/许可证/来源可信度分级，冲突不掩盖
5. **假设显式化（知何时承认）**：查不到就标注，绝不编造

## 任务分级（Triage）——"知"的第一步

执行任何动作前，先判定任务等级。**分级决定了流程走多重，是进化的核心。**

| 等级 | 判定信号 | 流程 | 预算 |
|------|----------|------|------|
| **T0·秒答** | 琐碎修改（改 typo/改名/单行）；纯概念解释；纯数学推导；用户明确说"不用查" | 直接回答，**不进流程**。仅当答案涉及事实/版本/时效时升级 T1 | — |
| **T1·轻流程** | 中等复杂度，预期 1-3 个缺口，低风险（不涉版本兼容/API/真实数据） | 轻量缺口扫描（5 类过一遍，不展开复查循环）→ 有高影响缺口才定向检索 → 快速判断，不做六维评分 | 每缺口 ≤2 轮检索 / ≤1 页 WebFetch |
| **T2·全流程** | 复杂代码/方案/调研；多缺口；高风险（涉版本/API/合规/数据） | 完整四步 + 复查循环 + 模板评分 + 复盘闭环 | 每缺口 ≤3 轮检索 / ≤2 页 WebFetch |

**分级判定法**（按顺序）：任务类型 → 上下文充分度 → 风险面（版本/API/数据/合规/规模）。拿不准时升一级，宁重勿漏。

## 四步工作流（T1/T2 按等级缩放）

### Step 1：缺口检测（Gap Detection）

扫描 5 类缺口（详细清单见 references/gap-detection.md）：

| 类别 | 示例 | 典型影响 |
|------|------|----------|
| A 版本与兼容性 | 依赖/语言/运行环境版本 | 高 |
| B API 与接口 | 签名、认证、废弃警告 | 高 |
| C 数据与输入 | 格式、编码、样例、质量 | 高/中 |
| D 领域知识 | 政策、时效性、专业术语 | 高（调研类） |
| E 现有资产 | 项目内可复用代码/工具函数/规范 | 高（P0 优先） |

每个缺口按影响评分：**高**（直接影响正确性，必须解决）/ **中**（影响质量，尽量解决否则标注）/ **低**（锦上添花，用默认值）。

**复查**（仅 T2）：对照任务目标查遗漏、查误报，最多 2 轮。

**前提拆解与反例检查（仅 T2）**——动手前拆隐含前提：
1. 拆解任务与拟用方案的**隐含前提**（如"假设依赖已装""假设用户用 PostgreSQL"）
2. 对每个前提**列反例**（什么情况下它不成立）
3. 写明**结论成立的条件**（满足什么才能按此执行）
4. 无法排除的反例 → 升为缺口，进入 Step 2 检索或 AskUserQuestion

**时效性强制**：任务涉**政策 / 价格 / 版本 / 法律 / 医疗 / 金融 / 新闻**等时效性信息 → **无论 T0/T1/T2，一律进入检索**，不凭预训练知识作答。

**T0/T1 出口**：无高影响缺口 → 立即退出本 Skill，直接执行。不要把流程做成负担。

### Step 2：资源路由（自适应委托）

按缺口类型路由（详细见 references/routing.md）：

```
项目内已有代码/配置 → Glob/Grep（P0，不联网）
API/版本/错误信息   → WebSearch + WebFetch 官方文档（P1）/ GitHub、SO（P2）
成熟开源库发现      → WebSearch GitHub + WebFetch README
行业数据/政策/选型  → 委托可用的调研 skill（见下方"自适应委托"）
```

**自适应委托（进化的核心）**——探测式，不硬绑：
1. 优先：委托 `domain-research`（若可用）
2. 其次：委托用户环境已安装的调研类 skill（如 `research`）
3. 兜底：降级为 WebSearch 2-3 组关键词多轮交叉，标注"未做多平台交叉验证"
4. **只委托一次**，不重复造检索轮子；委托结果缺失的缺口再定向补查

**检索预算（进化的核心，防烧上下文）**：
- 每缺口检索轮数：T1≤2 / T2≤3；每缺口 WebFetch 页数：T1≤1 / T2≤2
- 每轮检索关键词最多调整 3 次（第1轮主体+功能 → 第2轮加版本 → 第3轮换同义词/英文）
- **超预算 → 标记"未找到"，停止续搜，进入 Step 4 标注假设**。宁可承认查不到，不烧上下文。

**检索范围约束（防越界）**：只查与**当前任务直接相关**的缺口，不借题发挥扩展检索范围（如任务是修 bug，不顺便调研无关技术选型）。检索服务于任务，不服务好奇心。

### Step 3：融合校验（Cross-Validation）

- **多方案评分**（仅 T2；T1 用快速判断）：**硬约束 + 任务相关评分维度 + 默认权重模板**，不套全局固定公式（见 references/validation.md）：
  - **硬约束（一票否决）**：版本兼容；许可证允许性；高风险场景安全底线（加密/认证/密钥类必须查安全记录与 CVE）。
  - **任务相关维度**：按任务类型选模板——技术库选型（可复用性/维护状态/安全/社区/许可）、UI 组件（设计匹配/可访问性/bundle size/维护/许可）、安全敏感（安全审计/CVE/API 稳定性/维护/许可）等。
  - **权重是先验不是精算**：用于快速决策排序，不伪装科学精确；分差 <0.5 时优先维护性。
- **来源可信度（多轴判定，不单看 star）**：综合 5 轴——①来源权威性（官方/厂商 > 独立专家 > 活跃社区 > 个人）②新鲜度（有日期且近期）③版本匹配度（与项目版本匹配）④维护状态（持续维护 vs 停滞）⑤直接证据强度（一手源码/官方文档 > 二手转述）。**任一轴为关键负面（版本过时、官方已废弃、无维护）即降级**；star 多 ≠ 正确/维护/版本匹配/安全。
- **冲突处理**：外部 vs 原有假设 → 以权威来源为准并记录修正；来源间矛盾 → 并列呈现不擅自裁决。
- **验证循环**（≤2 次）：提取验证（是否正文）→ 完整性验证（是否含关键信息）→ 适用性验证（版本/依赖/许可）。
- **高风险双源复核**：结论涉及**生产环境 / 安全 / 合规 / 财务 / 法律** → 要求 **≥2 个独立来源**一致才采纳；仅 1 来源时明确标注"单一来源待验证"。

### Step 4：借力执行（Leverage & Execute）

- **复用模式**：直接引用 / 适配修改 / 包装调用 / 参考思路 / 全新编写（全新必须 Step 2 确认过"未找到"）。
- **假设显式化**：未解决缺口必须输出在"未解决的假设"中，标影响与建议确认方式。
- **输出要求**：包含"使用的现有资源"（来源+模式）+ "未解决的假设"。
- **证据分级输出**：每条关键结论标注认知状态——`✅已确认事实`（有来源/可验证）/ `🔶合理推断`（有依据未证实）/ `⚠️待验证假设`（无法确认）/ `❓未核实项`（没查到）。不确定就不写"确定"。
- 执行后快速自检：高影响缺口是否全部解决或标注、是否有编造、引用代码是否查过版本/许可、**是否有附和用户错误断言的成分**。

## 卡壳自动触发

| 信号 | 判定 | 动作 |
|------|------|------|
| 推理停滞 | 连续2轮"可能/大概/应该是" | 停止推理，转 Step 2 检索 |
| 报错循环 | 同一错误修3次未解决 | 搜错误信息，不再硬修 |
| 功能膨胀 | 从头实现复杂功能但未检索过 | 停止编码，搜现有库 |
| 信息漂移 | 输出偏离用户需求 | 回 Step 1 重新检测边界 |
| 方案纠结 | 2 方案间反复超2轮 | 用评分表强制决策 |
| 校验失败 | 同源验证2次不通过 | 换来源或标记未验证 |

## 复盘闭环（Retro Loop，进化的核心）

> 目标是真正的**跨会话自改进**，不是只生成复盘文本。闭环五步：

1. **记录**：T1/T2 执行完，自动把一行复盘 append 到 `~/.zhizhi/retro.md`（格式：日期 | 任务类型 | 分级 | 漏掉的缺口 | 错误的假设 | 下次改进 | 生效?）。
2. **读取**：每次 T1/T2 **启动前**，读取 retro 历史（最近 N=20 条）。
3. **提取重复失败模式**：同一缺口类别 / 同一假设模式 / 同一路由失败出现 **≥2 次** → 标记为"重复失败模式"。
4. **调整本次行为**：重复失败模式 → 注入本次执行——如"版本缺口连漏"→ 本次 Step 1 强制版本检查；"委托失效"→ 本次直接走降级策略。
5. **验证是否生效**：执行结束时对照本次是否仍犯同类错误，在 retro 里记 `改进生效/未生效`。

```
[retro] 日期 | 任务类型 | 分级 | 漏掉缺口 | 错误假设 | 下次改进 | 生效?
```

- 复盘是**闭环不是摆设**：没有"读取→提取→调整→验证"的 retro 只是日志，不算自改进。
- retro 文件是持久化状态，**自动写入**（这是跨会话自改进的前提）；路径 `~/.zhizhi/retro.md` 可用环境变量或用户偏好覆盖。
- **落地工具**：`scripts/retro.py` 实现 记录/读取/提取/建议 四命令（`python scripts/retro.py suggest` 直接给出本次注入动作，`patterns` 自动标出 ≥2 次的重复失败模式）。Windows 下用 `PYTHONUTF8=1 python scripts/retro.py ...` 防中文乱码。

## 协作地图

| 协作 skill | 场景 | 分工 |
|------------|------|------|
| `domain-research` / `research` | 调研/选型类缺口 | 探测到即委托，本 skill 只定义缺口与校验结果 |
| `doubt-driven-development` | T2 交付前的关键裁决 | 把重要假设/选型交给它做一次对抗性复查 |
| `grilling` | 用户想压力测试方案时 | 配合做前提挑战 |
| 前端/设计类 skill | 涉及 UI/视觉 | 检测到前端任务委托对应设计 skill，不重复实现 |

**边界**：本 skill 是"交通警察"，识别缺口、路由、校验、标注。不抢任何专门 skill 的活。

## 反模式（严禁）

无检索先编码 / 编造事实（"应该是"） / 封闭推理 / 无视文档 / 盲目复用（不查版本许可） / 重复检索（有调研 skill 却自己多平台搜） / 海量灌入（整页塞上下文） / 隐藏假设（不标注） / **盲目附和**（用户断言与证据矛盾时不验证即接受） / **检索越界**（查与任务无关的内容）。

> 详细案例见 references/anti-patterns.md。

## Trace（默认精简，--verbose 展开）

**默认只向用户输出**：`依据 / 未解决的假设 / 重要风险`（+ 复盘生效标记）。完整 trace 是**内部调试/评估机制**，不默认展示——避免让本来 100 字能说清的结果被元信息淹没。

需要调试时（用户说"显示 trace"/`--verbose`），才展开：

```
[trace] 任务: {描述} 分级: {T0/T1/T2}
[trace] Step1 缺口: {N}个（高H/中M/低L），复查{K}轮
[trace] Step2 路由: 委托={skill名}/直接={N}缺口/未解决={N}，预算用尽={N}
[trace] Step3 校验: 评分={X}/5，冲突修正={N}，未验证={N}
[trace] Step4 执行: 复用={模式}，假设={N}条
[trace] 卡壳触发: {次数}次（{类型}）
[retro] 生效: {是/否}
```

## References

- [triage.md](references/triage.md) — 任务分级判定细则
- [gap-detection.md](references/gap-detection.md) — 5 类缺口检测清单与评分
- [routing.md](references/routing.md) — 自适应路由、委托与检索预算
- [validation.md](references/validation.md) — 方案评分、来源可信度、冲突处理
- [anti-patterns.md](references/anti-patterns.md) — 反模式详解与案例
- [retro.md](references/retro.md) — 复盘方法与记录格式
